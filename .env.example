# Build server settings for scripts/deploy.sh.
# Copy to .env and adjust; .env is deliberately untracked (never committed).

SSH_DEST=german          # ssh alias from ~/.ssh/config, or user@host
#SSH_PORT=22
#REMOTE_DIR=~/build/sni-router
